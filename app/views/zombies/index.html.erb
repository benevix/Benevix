<p id="notice"><%= notice %></p>

<h1>Zombies</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Bio</th>
      <th>Apodrecendo</th>
      <th>Salario</th>
      <th>Idade</th>
      <th>Nascimento</th>
      <th>Zombification</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @zombies.each do |zombie| %>
      <tr>
        <td><%= zombie.nome %></td>
        <td><%= zombie.bio %></td>
        <td><%= zombie.apodrecendo %></td>
        <td><%= zombie.salario %></td>
        <td><%= zombie.idade %></td>
        <td><%= zombie.nascimento %></td>
        <td><%= zombie.zombification %></td>
        <td><%= link_to 'Show', zombie %></td>
        <td><%= link_to 'Edit', edit_zombie_path(zombie) %></td>
        <td><%= link_to 'Destroy', zombie, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Zombie', new_zombie_path %>
